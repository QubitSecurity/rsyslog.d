##### 00-imfile #####

# Load imfile module (file input).
# Required only if you use input(type="imfile") elsewhere.
module(load="imfile")

# Optional: polling interval for imfile inputs (seconds).
# $InputFilePollInterval 5
